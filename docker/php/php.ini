; Slightly above the 10MB app limit so oversized files reach Laravel validation (422)
; instead of being cut off by PHP.
upload_max_filesize = 12M
post_max_size = 13M
memory_limit = 256M
date.timezone = UTC
expose_php = Off
